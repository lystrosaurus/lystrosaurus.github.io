---
title: 'CSS Grid 布局速查'
description: 'CSS Grid 的核心属性与常用布局模式'
category: '前端'
order: 1
tags: ['CSS', '布局']
---

## 基本概念

CSS Grid 是二维布局系统，同时控制行和列。

```css
.container {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
}
```

## 容器属性

### grid-template-columns / rows

```css
/* 固定值 */
grid-template-columns: 200px 1fr 200px;

/* repeat */
grid-template-columns: repeat(3, 1fr);

/* auto-fill / auto-fit */
grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
```

### gap

```css
gap: 1rem;           /* 行列间距 */
row-gap: 1rem;       /* 行间距 */
column-gap: 1rem;    /* 列间距 */
```

### justify-items / align-items

```css
justify-items: center;  /* 水平居中 */
align-items: center;    /* 垂直居中 */
place-items: center;    /* 同时设置 */
```

## 项目属性

### grid-column / grid-row

```css
.item {
  grid-column: 1 / 3;    /* 跨列 */
  grid-row: span 2;       /* 跨行 */
}
```

### place-self

```css
.item {
  place-self: center;  /* 单个项目居中 */
}
```

## 常用布局

### 圣杯布局

```css
.layout {
  display: grid;
  grid-template-columns: 200px 1fr 200px;
  grid-template-rows: auto 1fr auto;
  min-height: 100vh;
}
```

### 卡片网格

```css
.cards {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 1.5rem;
}
```
